<%= form_with(model: channelcomment, local: true) do |form| %>
  <% if channelcomment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(channelcomment.errors.count, "error") %> prohibited this channelcomment from being saved:</h2>

      <ul>
      <% channelcomment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comment_id %>
    <%= form.number_field :comment_id %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
