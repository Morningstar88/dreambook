<%= form_with(model: channelpost, local: true) do |form| %>
  <% if channelpost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(channelpost.errors.count, "error") %> prohibited this channelpost from being saved:</h2>

      <ul>
      <% channelpost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_area :link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
